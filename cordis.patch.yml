# dsh-ui-peak-rate bundle: inserts the peak-rate client plugin row.
# Applied when a profile lists this bundle in dsh.profile.bundles.
- insert:
    - id: dsh-ui-peak-rate
      name: dsh-ui-peak-rate
